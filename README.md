# pr-practice

A tiny calculator library used to practice the GitHub pull request workflow.

## Functions

- `add(a, b)` — returns the sum of two numbers
- `subtract(a, b)` — returns the difference of two numbers

## Running tests

```
python -m pytest
```
